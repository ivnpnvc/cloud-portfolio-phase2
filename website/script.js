document.addEventListener("DOMContentLoaded", () => {
  const statusElement = document.getElementById("status");
  if (statusElement) {
    statusElement.textContent = "Website erfolgreich geladen";
  }
  console.log("Cloud Architecture Demo Page loaded successfully.");
});
